Cuando descargues la carpeta, cambia su nombre a .vscode (esto es, escribe un punto al inicio del nombre). Sin esto, VSCode no la va a reconocer. 

Si ya cuentas con ambos archivos en tu proyecto de VSCode, entonces podrías copiar el código.
